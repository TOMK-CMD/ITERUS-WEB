# apps/web — Iterus website (Next.js)

The Next.js App Router application behind iterus.cz / iterus.io. Everything you need to know
lives at the repository root: `CLAUDE.md` (how we work), `docs/ARCHITECTURE.md`,
`docs/RUNBOOK.md`, `specs/`. Run commands from the root (`pnpm dev`, `pnpm check`, …).
