packages:
  - apps/*
  - packages/*

# pnpm 10 refuses to run postinstall scripts unless approved; these two are generated by
# create-next-app and are safe to leave unbuilt (prebuilt binaries are used).
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
